"""
Skills package: one module per skill, each registering tools with the agent.

Skills are loaded by the tool registry (neuroion.core.agent.tool_registry)
via _load_skill_modules(). Add a new .py file here to register new tools;
do not use leading underscore in the module name.
"""
